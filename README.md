
## English

`TanaAutoWatcher.ps1` executes `TanaPatch.ps1` through _**app-updates events**_ —_-+
- It is recommended to put `TanaAutoWatcher.ps1` into the _**startup item**_ or _**task scheduler**_

> Other files are of no use, currently.

## 中文

`TanaAutoWatcher.ps1` 通过 _**app 更新事件**_，执行 `TanaPatch.ps1` —_-+
- 推荐将 `TanaAutoWatcher.ps1` 放入 _**开机启动项**_ 或 _**任务计划程序**_

> 其他文件暂时没有用.
